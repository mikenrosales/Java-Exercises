interface Animal { // Animal is an interface class, adding public keyword to interface classes is redundant
    // An abstract class allows you to create functionality that subclasses can implement or override. An interface only allows you to define functionality, not implement it.
    void name(); // interface methods defined without a body/functionality
    void sound();
}